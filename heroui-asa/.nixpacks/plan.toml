# Additional Nixpacks configuration

# Explicitly specify the providers
providers = ["node"]

# Specify exact Node.js version and yarn
[phases.setup]
nixPkgs = [
  { pkg = "nodejs", version = "20" },
  { pkg = "yarn" }
]

# Environment variables for the entire build process
[variables]
NODE_ENV = "development"

# Override environment variables for specific phases
[phases.build.variables]
NODE_ENV = "production"

# Add custom build commands
[phases.build.cmds]
"build-app" = "yarn build"
"cleanup" = "yarn cache clean"
